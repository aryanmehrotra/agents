# local env — may contain API keys (tracked: .env.example, .env.local)
**/configs/.env

# go build output
bin/
*.exe
*.test
*.out

# editor / OS
.DS_Store
.idea/
.vscode/

# compiled binaries (go build . emits one named after the module directory)
/*/*-agent
orchestrator/orchestrator
localtest/*/claude-openai-shim
agents/sdlc/breaking-change-agent/breaking-change-agent

# sql-agent's seeded SQLite file, created at runtime
sql-agent/*.db
